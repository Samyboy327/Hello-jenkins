pipeline {
    agent any

    stages {
        stage('Hello World') {
            steps {
                echo 'Hello World'
            }
        }

        stage('List Repository Files') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
